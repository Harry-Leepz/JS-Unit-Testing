# **JavaScript Unit Testing (Jest/Vitest)**

## **Installing Vitest**

```
  npm install --save-dev vitest
```

Update the `package.json` file with vitest added to the scripts.

```
  "scripts": {
    "test": "vitest --globals"
  }
```
